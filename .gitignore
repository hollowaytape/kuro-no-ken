original/
*.xlsx
*.sublime-project
*.sublime-workspace
*.bin
states/
test_reports/

# Python
__pycache__/
*.pyc

# Build output and backups - regenerated by reinsert.py / build.py
patched/
patched_backup/
*.hdi

# Other projects' material and local reference
other Forest/

# Autoplay run markers (the *_map.json route files are inputs to map_report.py)
routes/*.stop

# Local screenshots, and one-off investigation scripts (the reusable ones are in tools/)
img/
scratch_emu/
